using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DetermineSoliderLocations : MonoBehaviour
{
    public GameObject placeHolder;
    [SerializeField] int numberSoliders = 16;
    [SerializeField] float soliderSize = 1;

    // Start is called before the first frame update
    void Start()
    {
        int level = 2;
        for(int i = 0; i < numberSoliders; i++)
        {

        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
